[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Safe pedestrian route recommendation project backend"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.52.4",
]
